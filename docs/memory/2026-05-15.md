# 2026-05-15 工作存档

## Session Start Context

### 外部工程系统状态
- Active Step: Step 9 — Correction Download Frontend Integration（进行中）
- Step 9 完成后：端到端链路打通
- 下一步：P2 决策或 Step 10

### 本次 Session 目标
完成 Step 9 端到端调试与四件套对账收尾

---

## 本日完成事项

### Step 9 端到端调试（完成）

**问题发现**：
1. DeepSeek API key 无效 (`sk-a2fb983c14e54ca68409923b6373fcb1` → "Authentication Fails")
2. DocumentRepository.find_by_id 返回的 Document.user_id 是 string，current_user.id 是 UUID，导致比较失败
3. 修正 endpoint 返回 404 而不是 403（因为 404 在 403 之前被检查）

**修复执行**：
1. 创建 `backend/.env` 文件，设置 DEEPSEEK_API_KEY 等配置
2. 修改 `document_repository.py` 的 `find_by_id` 方法，将 `row.user_id` 转换为 UUID：`user_id=UUID(row.user_id)`
3. 验证修复后 correction endpoint 返回 202，correction job 完成

**验证结果**：
- parse-spec → validate-with-spec → get-report → create-correction → poll-correction → completed
- Correction job 状态从 pending → completed，输出文件 `uploads/{report_id}_corrected.docx`

---

## Step 9 完成记录

**完成时间**：2026-05-15 04:45

**变更文件清单**：
- `backend/.env` — 新建（DEEPSEEK_API_KEY 等配置）
- `backend/app/infrastructure/persistence/document_repository.py` — find_by_id UUID 类型修复
- `backend/app/api/endpoints/corrections.py` — （无需修改，问题在 repository）

**Definition of Done 满足情况**：
- [x] 后端 validate-with-spec 返回 200 且数据持久化
- [x] GET /spec/reports/{report_id} 返回完整报告
- [x] POST /corrections/ 创建 correction job 返回 202
- [x] GET /corrections/{job_id} 轮询返回 completed
- [x] GET /corrections/{job_id}/download 返回 corrected 文件

**四件套对账状态**：
- [x] docs/progress.md — 已更新（Step 9 ✅ 完成）
- [x] docs/knowledge/PaperNormAI-knowledge/900-learning-log.md — 已追加
- [x] docs/knowledge/PaperNormAI-knowledge/910-skill-run-log.md — 已追加
- [x] docs/memory/2026-05-15.md — 本文件

---

## Step 10 完成记录

**完成时间**：2026-05-15 05:10

**变更文件清单**：
- `backend/app/application/exceptions.py` — 新建（AppException, NotFoundError, AccessDeniedError, ValidationError）
- `backend/app/application/spec/spec_application_service.py` — 新建（SpecApplicationService）
- `backend/app/application/correction/correction_application_service.py` — 新建（CorrectionApplicationService）
- `backend/app/api/endpoints/spec_validation.py` — 重构为薄 endpoint（仅 HTTP 解析 + 响应映射）
- `backend/app/api/endpoints/corrections.py` — 重构为薄 endpoint（仅 HTTP 解析 + 响应映射）

**Definition of Done 满足情况**：
- [x] spec_validation.py 明显变薄（0 infrastructure imports，2 application imports）
- [x] corrections.py 变薄（仅 background task 保留 infrastructure）
- [x] 事务从 API 下沉到 application service
- [x] 权限下沉：endpoint 传 current_user，application service 做所有权判断
- [x] architecture-check 通过

**四件套对账状态**：
- [x] docs/progress.md — 已更新（Step 10 ✅ 完成）
- [x] docs/knowledge/PaperNormAI-knowledge/900-learning-log.md — 已追加
- [x] docs/knowledge/PaperNormAI-knowledge/910-skill-run-log.md — 已追加
- [x] docs/memory/2026-05-15.md — 本文件

---

## 技术债务（更新）

| # | 项目 | 优先级 | 状态 |
|---|------|--------|------|
| 1 | application 层下沉重构（API → application） | ✅ 已完成 | Step 10 |
| 2 | Alembic 数据库迁移路径建立 | 低 | 待处理 |
| 3 | DeepSeek API Key 更新 | 高 | 待处理 |

---

## 下一步行动

1. **P2 决策**：
   - P2A：前端继续扩展（修正文档预览）
   - P2B：治理重构（API → application 下沉重构）
2. **技术债务**：DeepSeek API key 失效问题需调查

---

## 决策记录

| # | 决策 | 理由 |
|---|------|------|
| 1 | 在 document_repository.py 中将 row.user_id 转换为 UUID | 保持 Document 实体类型一致性 |
| 2 | 使用 raw SQL 避免 PGUUID SQLite 绑定问题 | SQLAlchemy ORM 无法处理 UUID→str 的隐式转换 |

---

## 卡点与风险

| # | 卡点 | 状态 |
|---|------|------|
| 1 | DeepSeek API key 失效 | 需更新 key 或切换 provider |
| 2 | AI 功能无法正常工作（rule extraction 返回 0 rules） | API key 问题 |