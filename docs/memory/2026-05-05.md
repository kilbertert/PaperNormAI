# 2026-05-05 工作存档

## 今日完成

### Phase 1 核心链路完成

1. **KG-1 验证通过** — Docling DOCX 解析保真度
   - temp.docx (390段落, 59章节) 解析正常
   - sections 和 paragraphs 分离正确
   - 分层结构（1/2/3级标题）正确识别

2. **KG-2 完成** — 接入应用层
   - `spec_validation.py` 替换旧链路为 AI 服务
   - DoclingDocumentParser + RuleExtractionService + SemanticValidationService

3. **KG-3 通过** — 长文档处理
   - 390段落文档正常处理，无截断或性能问题

4. **DeepSeek 集成完成**
   - 配置: `AI_PROVIDER=deepseek`
   - API: `sk-a2fb983c14e54ca68409923b6373fcb1`
   - Base URL: `https://api.deepseek.com/v1`
   - Model: `deepseek-chat`

### 修复的问题

1. **ViolationDetail dataclass 字段顺序错误**
   - non-default 字段在 default 字段后面导致 TypeError
   - 修复：将 id 移到最后，所有必填字段在前

2. **ValidationReport dataclass 字段顺序错误**
   - same issue as above

3. **_parse_violations 解析逻辑错误**
   - `[违规]` 被错误识别为 section marker 导致 pending_violation_data 被清空
   - 修复：调整判断顺序，`[校验结果]` 优先于 `[违规]`

4. **OpenAIProvider 多 provider 支持**
   - 添加 deepseek 和 ollama 配置
   - 根据 `ai_provider` 设置选择对应的 api_key/model/base_url

## 端到端测试结果

```
Doc: 390 paragraphs, 59 sections
Extracted: 17 rules
Validation: 30 violations
  - Errors: 30
  - Warnings: 0

违规示例:
  [error] para=1: [font] 必须: 正文字体应为宋体
  [error] para=2: [font] 必须: 正文字体应为宋体
  ...
```

## 下一步

1. KG-4: 规则和 ValidationReport 持久化（数据库）

## 关键教训

**AI 响应解析必须先测试简单 case：** 解析器逻辑bug在简单 case 下更容易发现。

**多 provider 架构需要统一配置管理：** 在 config.py 中集中管理所有 provider 的配置。

---

## 下午更新：Phase 2 完成 (15:00)

### KG-6 Phase 2 完成

扩展 DocumentModel 和 DoclingDocumentParser 支持表格/插图/公式解析：

**新增 Dataclasses:**
- `TableInfo`: rows, cols, caption, style
- `FigureInfo`: width, height, caption
- `FormulaInfo`: content, numbered, number

**验证结果 (temp.docx):**
```
Tables: 6
Figures: 13
Formulas: 20
```

Richard Review: **PASSED** (no Must Fix issues)

### 更新的文件
- `backend/app/infrastructure/docling/document_model.py`
- `backend/app/infrastructure/docling/parser.py`

### Minor Observations (非阻塞)
1. Formula extraction uses `type().__name__` instead of `isinstance()` — fragile but works
2. No hasattr guards for `doc.tables`, `doc.pictures` — could fail on unexpected docling versions
3. Nested loop in formula extraction — acceptable for current scope

---

*存档时间: 2026-05-05 15:00*
*更新: docs/progress.md, handoff/BUILD-LOG.md, handoff/REVIEW-REQUEST.md*